# ── App (Streamlit frontend + inference layer) ─────────────────────────────────
streamlit>=1.28.0

# Core ML/NLP
# sentence-transformers==2.7.0  # Too heavy for Cloud (pulls PyTorch ~2GB) — app falls back to TF-IDF
scikit-learn==1.4.0
textblob==0.18.0
numpy==1.26.4
pandas==2.2.0

# Sentiment analysis
nltk>=3.8.0
vaderSentiment>=3.3.2

# Config
PyYAML>=6.0

# Utilities
python-dotenv==1.0.0
joblib==1.3.2
tqdm==4.66.0

# ── Pipeline-only (not required to run the Streamlit app) ─────────────────────
# Uncomment if running pipeline.py locally:
# pyarrow>=14.0.0
# requests==2.31.0
# praw==7.7.1
# fastapi==0.110.0
# uvicorn==0.27.0
# matplotlib==3.8.0
# seaborn==0.13.0
